name = "statedoku-twitter-bot"
main = "src/worker.js"
compatibility_date = "2024-09-23"

# Cron — DISABLED June 19, 2026 (user paused daily auto-publishing).
# The worker code still runs on demand via the /fetch handler (?key=MANUAL_TRIGGER_KEY)
# so manual tweets remain possible. Re-enable by uncommenting the crons line.
[triggers]
crons = []
# crons = ["0 13 * * *", "0 23 * * *"]   # 2 tweets/day, US morning + after-work

# ─────────────────────────────────────────────────────────────────────────
# Secrets to set (NEVER commit these to git):
#
#   wrangler secret put TWITTER_API_KEY
#   wrangler secret put TWITTER_API_SECRET
#   wrangler secret put TWITTER_ACCESS_TOKEN
#   wrangler secret put TWITTER_ACCESS_TOKEN_SECRET
#   wrangler secret put ANTHROPIC_API_KEY
#   wrangler secret put MANUAL_TRIGGER_KEY    # random string for manual testing
#
# Get Twitter keys at:    https://developer.twitter.com
# Get Anthropic key at:   https://console.anthropic.com
# ─────────────────────────────────────────────────────────────────────────
